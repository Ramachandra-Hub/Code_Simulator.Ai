import { CareerOSShell } from "@/components/career-os/career-os-shell";

export default function CareerOSLayout({ children }: { children: React.ReactNode }) {
  return <CareerOSShell>{children}</CareerOSShell>;
}
